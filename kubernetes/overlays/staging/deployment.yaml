apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloud-app
  template:
    metadata:
      labels:
        app: cloud-app
    spec:
      containers:
        - name: cloud-app
          image: asia-east1-docker.pkg.dev/indoornav-462516/hello-repo/cloud-app:latest
          ports:
            - containerPort: 3000
